import Link from 'next/link';
import RequestPasswordReset from '../components/RequestPasswordReset';

const request_password_reset = props => (
    <RequestPasswordReset/>
);
export default request_password_reset;